import { Coda } from 'next/font/google';
import { Poppins } from 'next/font/google';

export const HeadingFont = Coda({ 
    subsets: ['latin'], 
    weight: '400'
});

export const TextFont = Poppins({ 
    subsets: ['latin'],
    weight: '400'
});
